info:
  name: Patch request renaming name
  type: http
  seq: 5

http:
  method: PATCH
  url: http://127.0.0.1:3000/users/1
  body:
    type: json
    data: |-
      {
        "name":"new"
        
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
